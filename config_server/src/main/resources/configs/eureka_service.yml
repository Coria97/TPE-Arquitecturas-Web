server:
  port: 8761

spring:
  application:
    name: eureka_service
  profiles:
    active: native

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/